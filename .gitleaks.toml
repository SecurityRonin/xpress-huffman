# gitleaks configuration — extends the default ruleset. No project-specific
# allowlist entries: this crate ships only code, docs, and public test vectors,
# so real secrets surface against the upstream rules.
[extend]
useDefault = true
