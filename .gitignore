/target
# Library crate: the lockfile is not committed (downstreams resolve their own).
Cargo.lock

# mkdocs build output (generated by `mkdocs build`; the Pages workflow builds it in CI)
/site/

# cargo-fuzz build output and evolved corpora/crash artifacts (standalone workspace)
/fuzz/target
/fuzz/corpus
/fuzz/artifacts
/fuzz/Cargo.lock

# OS / editor cruft
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
